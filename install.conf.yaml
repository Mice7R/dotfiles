- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.tmux.conf: tmux.conf
    ~/.links/links.cfg:
        create: true
        path: links/links.cfg
    ~/.bin/urldecode: bin/urldecode
    ~/.inputrc: inputrc
    ~/.config/ranger: ranger
